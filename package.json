{
  "name": "imtakt",
  "private": true,
  "workspaces": [
    "packages/*",
    "mcp"
  ],
  "scripts": {
    "typecheck": "bun run --filter './packages/*' typecheck && bun run --filter './mcp' typecheck",
    "build": "bun run --filter './packages/*' build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
