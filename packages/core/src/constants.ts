/** Hosted ImTakt Server — default for MCP, SDK, and CLI (adoption path). */
export const IMTAKT_HOSTED_API_URL = "https://api.imtakt.dev"

/** Harness stderr warning when neither GTFS-RT feed nor per-leg realtime is present. */
export const SCHEDULE_ONLY_WARNING =
  "Schedule only — realtime not active on this result; recheck closer to departure"
