# Local override when testing MCP/CLI against a dev API (default: https://api.imtakt.dev)
# IMTAKT_SERVER_URL=http://localhost:3011

# ImTakt Server env lives in imtakt-router/.env.example (ops/contributors)
